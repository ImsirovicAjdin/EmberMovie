{{#if @hasBlock}}
    {{yield stars}}
{{else}}
{{#each @stars as |star|}}
<button class="rating-star" {{on "click" (fn this.star.rating "setRating")}}>
    {{#if @star.isFull}} {{fa-icon "star"}} 
    {{else}} 
    {{fa-icon "star-o"}} 
    {{/if}}
</button>
{{/each}}
{{/if}}
