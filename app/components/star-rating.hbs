{{#if @hasBlock}}
    {{yield stars}}
{{else}}
{{#each @stars as |star|}}
<button class="rating-star" {{on "click" "setRating" this.star.rating}}>
    {{#if @star.isFull}} {{fa-icon "star"}} 
    {{else}} 
    {{fa-icon "star-o"}} 
    {{/if}}
</button>
{{/each}}
{{/if}}
